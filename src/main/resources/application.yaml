spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:book_management
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
